name: eagle_eye
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.13+14

environment:
  sdk: ^3.5.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  #UI
  flutter_screenutil: ^5.9.3
  country_code_picker: ^3.3.0
  flutter_svg: ^2.0.17
  fluttertoast: ^8.2.12
  gap: ^3.0.1
  modal_progress_hud_nsn: ^0.5.1
  carousel_slider: ^5.0.0
  shimmer: ^3.0.0
  sliding_up_panel_custom: ^2.1.1+1
  textfield_tags: ^3.0.1
  dropdown_button2: ^2.3.9
  cherry_toast: ^1.12.0


  #Image
#  image_cropper: ^9.1.0
  image_picker: ^1.1.2
  file_picker: ^10.0.0
  cached_network_image: ^3.4.1
#  widget_zoom: ^0.0.4

  #Videoplayer
  chewie: ^1.11.3
  video_player: ^2.9.5
  get_thumbnail_video: ^0.7.3

  #Functionality
  url_launcher: ^6.3.1
  package_info_plus: ^8.3.0
  intl: ^0.20.2
  camera: ^0.11.0+2
  path_provider: ^2.1.5
#  awesome_watermark: ^0.0.1
  connectivity_plus: ^6.1.2
#  mobile_device_identifier: ^0.0.3
  webview_flutter:
  app_tracking_transparency: ^2.0.6+1


  #State management

  bloc: ^9.0.0
  flutter_bloc: ^9.1.0
  freezed_annotation: ^3.0.0

  #Local data
  shared_preferences: ^2.5.3

  #Map
  mapbox_maps_flutter: ^2.4.1
  permission_handler: ^11.4.0
  geolocator: ^13.0.4
  location: ^8.0.0
  #  mapbox_search: ^4.3.1
  #  google_places_flutter: ^2.0.9
  #  flick_video_player: ^0.9.0
  #Notification
  onesignal_flutter: ^5.3.0

  #social sign in
  google_sign_in: ^6.3.0
  sign_in_with_apple: ^7.0.1

  #Firebase
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  firebase_analytics: ^11.4.0
  firebase_crashlytics: ^4.3.0

  #API calling
  dio: ^5.8.0+1
  http: ^1.3.0
  socket_io_client: ^3.1.2
  app_links: ^6.4.0
  flutter_branch_sdk: ^8.4.1
  get: ^4.7.2
  readmore: ^3.0.0
  geocoding: ^3.0.0
  

  logic_go_network:
    git:
      url: https://bitbucket.org/LogicGoNIkunj/logic_go_network.git
      ref: main
  #  logicgo_network_flutter:
  #    git:
  #      url: https://github.com/rohitlogicgo/logicgo_network_flutter.git
  #      ref: main

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  share_plus: ^10.1.4
  lottie: ^3.3.1
#  ffmpeg_kit_flutter:
#  ffmpeg_kit_flutter:
#    git:
#      url: https://github.com/Sahad2701/ffmpeg-kit.git
#      path: flutter/flutter
#      ref: flutter_fix_retired_v6.0.3
#  image: ^4.5.3
  device_info_plus: ^11.3.0
  country_picker: ^2.0.27
#  video_compress: ^3.1.4
  light_compressor_v2: ^1.0.0
  rating_dialog: ^2.0.4
#  in_app_review: ^2.0.10
  showcaseview: ^4.0.1
  firebase_performance: ^0.10.1+7
  animated_rating_bar: ^0.0.8
  youtube_player_flutter: ^9.1.1
  firebase_remote_config: ^5.4.3
  cupertino_tabbar: ^2.0.0
  google_mobile_ads: ^5.2.0
  internet_connection_checker: ^1.0.0+1
  timeago: ^3.2.2


dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  flutter_gen_runner: ^5.10.0
  change_app_package_name: ^1.5.0
  camera_android_camerax: ^0.6.14+1
  freezed: ^3.0.6


flutter_gen:
  integrations:
    flutter_svg: true
    lottie: true

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/icons/
    - assets/images/
    - assets/videos/
    - assets/fonts/
    - assets/animation/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  fonts:
    - family: roboto
      fonts:
        - asset: assets/fonts/roboto/Roboto-Light.ttf
          weight: 300
        - asset: assets/fonts/roboto/Roboto-Regular.ttf
          weight: 400
        - asset: assets/fonts/roboto/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/roboto/Roboto-Medium.ttf
          weight: 600
        - asset: assets/fonts/roboto/Roboto-Bold.ttf
          weight: 700
        - asset: assets/fonts/roboto/Roboto-Black.ttf
          weight: 800
    - family: test_tiempos_headline
      fonts:
        - asset: assets/fonts/test_tiempos_headline/TestTiemposHeadline-Black.otf
          weight: 300
        - asset: assets/fonts/test_tiempos_headline/TestTiemposHeadline-Regular.otf
          weight: 400
        - asset: assets/fonts/test_tiempos_headline/TestTiemposHeadline-Medium.otf
          weight: 500
        - asset: assets/fonts/test_tiempos_headline/TestTiemposHeadline-Semibold.otf
          weight: 600
        - asset: assets/fonts/test_tiempos_headline/TestTiemposHeadline-Bold.otf
          weight: 700
        - asset: assets/fonts/test_tiempos_headline/TestTiemposHeadline-Black.otf
          weight: 800
    - family: alice
      fonts:
        - asset: assets/fonts/alice/Alice-Regular.ttf
          weight: 400
        

  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
